# Handles Foursquare JSON. Takes in a JSON string and returns a dictionary mapping
# relevant categories to their values
import json # build-in Python library

def handler(jsonString):
    # http://docs.python.org/2/library/json.html
    # handy resource
